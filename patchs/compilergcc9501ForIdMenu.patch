Index: compilergcc/compilergcc.cpp
===================================================================
--- compilergcc/compilergcc.cpp	(révision 9501)
+++ compilergcc/compilergcc.cpp	(copie de travail)
@@ -310,7 +310,8 @@
     m_RunProjectPostBuild(false),
     m_IsWorkspaceOperation(false),
     m_IsCompileFileRequest(false),
-    m_LogBuildProgressPercentage(false)
+    m_LogBuildProgressPercentage(false),
+    m_IdwxEvent(0)
 {
     if (!Manager::LoadResource(_T("compiler.zip")))
         NotifyMissingFile(_T("compiler.zip"));
@@ -621,6 +622,7 @@
 void CompilerGCC::Dispatcher(wxCommandEvent& event)
 {
     int eventId = event.GetId();
+    m_IdwxEvent = eventId;
 
 //    Manager::Get()->GetMessageManager()->Log(wxT("Dispatcher")));
 
@@ -1547,7 +1549,7 @@
 {
     if (m_CommandQueue.GetCount() == 0)
     {
-        CodeBlocksEvent evt(cbEVT_COMPILER_STARTED, 0, m_pProject, 0, this);
+        CodeBlocksEvent evt(cbEVT_COMPILER_STARTED,  m_IdwxEvent, m_pProject, 0, this);
         Manager::Get()->ProcessEvent(evt);
 
         if (clearLog)
@@ -1565,7 +1567,7 @@
 {
     if (m_CommandQueue.GetCount() == 0)
     {
-        CodeBlocksEvent evt(cbEVT_CLEAN_PROJECT_STARTED, 0, m_pProject, 0, this);
+        CodeBlocksEvent evt(cbEVT_CLEAN_PROJECT_STARTED,  m_IdwxEvent, m_pProject, 0, this);
         evt.SetBuildTargetName(target);
         Manager::Get()->ProcessEvent(evt);
     }
@@ -1576,7 +1578,7 @@
 {
     if (m_CommandQueue.GetCount() == 0)
     {
-        CodeBlocksEvent evt(cbEVT_CLEAN_WORKSPACE_STARTED, 0, 0, 0, this);
+        CodeBlocksEvent evt(cbEVT_CLEAN_WORKSPACE_STARTED,  m_IdwxEvent, 0, 0, this);
         Manager::Get()->ProcessEvent(evt);
     }
     Manager::Yield();
@@ -3764,7 +3766,7 @@
         if (manager->GetIsRunning() == this)
             manager->SetIsRunning(NULL);
 
-        CodeBlocksEvent evt(cbEVT_COMPILER_FINISHED, 0, m_pProject, 0, this);
+        CodeBlocksEvent evt(cbEVT_COMPILER_FINISHED, m_IdwxEvent, m_pProject, 0, this);
         evt.SetInt(m_LastExitCode);
         Manager::Get()->ProcessEvent(evt);
         m_LastExitCode = 0;
Index: compilergcc/compilergcc.h
===================================================================
--- compilergcc/compilergcc.h	(révision 9501)
+++ compilergcc/compilergcc.h	(copie de travail)
@@ -321,6 +321,9 @@
         size_t m_CurrentProgress;
         bool   m_LogBuildProgressPercentage;
 
+  private :
+        // id wxCommandEvent
+        int m_IdwxEvent;
         DECLARE_EVENT_TABLE()
 };
 
